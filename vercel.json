{
  "buildCommand": "npm run build && npm --prefix server install",
  "outputDirectory": "dist",
  "framework": "vite",
  "regions": ["iad1"],
  "functions": {
    "server/**": {
      "memory": 1024,
      "maxDuration": 30
    }
  }
}
